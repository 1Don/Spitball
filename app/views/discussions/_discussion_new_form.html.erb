<div class="row form-group  small">
      <div class="col-6">
        <%= form_for @discussion, html: { multipart: true } do |f| %>
            <%= f.label :category %>
            <%= f.select :category,
                        options_for_select(
                          [
                            "Fundraising",
                            "Prototyping and Testing",
                            "Marketing",
                            "Teambuilding",
                            "Legal",
                            "Industry Related",
                            "Other"
                            ]
                        ),
                        {},
                        class: 'form-control form-control-sm' %>
      </div>


  <div class="form-group">
    <%= f.label :title, "Call to Action/Problem Statement" %><span data-toggle="tooltip" data-placement="right" title="What do you want to ask?"class="pl-1 question-marker-ps"><%= fa_icon('question') %></span>
    <%= f.text_field :title, class: 'form-control form-control-sm', id: "focus-field"  %>
  </div>

  <div class="form-group">
    <%= f.label :content, "Description" %><span data-toggle="tooltip" data-placement="right" title="Provide extra context here if you need to"><%= fa_icon('question') %></span>
    <%= f.text_area :content, class: 'form-control form-control-sm', style: "resize: none;", :rows => 8 %>
  </div>
  <div class="row">
    <div class="col-md-5 col-xs-12">
      <!-- I want to use logo's text inside of button -->
      <%= f.submit "Spitball", class: "wadnewbutton d-none d-md-block" %>
      <%= f.submit "Spitball", class: "wadnewbutton mx-auto d-block d-sm-none" %>
    </div>
  </div>
<% end %>
