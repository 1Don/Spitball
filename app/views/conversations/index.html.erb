<div class=”ui segment”>
 <h3>Start a conversation</h3>
	 <div class=”ui list”>
	  <div class=”item”>



<div class=”ui segment”>
  <h3>Start a Chat</h3>
    <div class=”ui list”>
      <% @users.each do |user| %>
        <% if user.id != current_user.id %>
            <div class=”item”>
               <%= user.first_name %> <%= link_to "Start a conversation", conversations_path(:user_id => user.id), :method => :post%>
            </div>
        <% end %>
      <% end %>
    </div>
</div>









