<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,500,700,900" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all',                                              'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= render 'layouts/shim' %>
    <meta content='summary' name='twitter:card'/>
    <meta property="og:title" content="Spitball Site" />
    <meta property="og:description" content="Spitball Site" />
    <meta property="og:url" content="http://spitball.io/" />
    <meta property="og:image" content="http://spitball.io/assets/logo.png" />
    <meta property="og:type" />
    <meta name="twitter:card" content="summary" />
    </head>

  <body class="<%= params[:controller] %> <%= params[:action] %>" style="background-color: #FAFAFA;height:100%;">
    <div class=""><%= render 'layouts/header' %></div>

    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>">
        <div class="">
          <%= message %>
        </div>
      </div>
    <% end %>

    <div class="container-fluid px-0" style="height:100%">
      <%= content_for?(:sub_layout) ? yield(:sub_layout) : yield %>
    </div>

  <%#  <%= debug(params) if Rails.env.development? %>

  </body>
</html>
