<% content_for :sub_layout do %>
<div class="row no-gutters" style="height: 100vh;">
    <div class="col-md-2 border-right mt-4">
  	  <%= render "sidebar" %>
      <div id="docuz" class="accordion">
        <%= render "other_sidebar" %>
      </div>
    </div>
  	<div class="col-md-8 col-xs-12 mt-5 d-none d-sm-block">
    	<%= yield %>
    </div>
    <div class="col-md-7 col-xs-12 d-block d-sm-none">
      <%= yield %>
    </div>
    <div class="col-md-2 border-left mt-4 d-none d-sm-block">
      <%= render 'other_sidebar' %>
    </div>
  </div>
<% end %>

<%= render template: 'layouts/application' %>
