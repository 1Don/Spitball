<div class="row">
  <div class="col-6 mx-auto">
    <div class="row">
      <div class="col-12">
        <h1> What's on your mind?</h1><br>
      </div>

      <div class="col-9 mx-auto">
        <div class="embed-responsive embed-responsive-16by9">
          <iframe class="embed-responsive-item" src="https://www.youtube.com/embed/LT-WH4kW-RQ" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
        </div>

        <p class="tips-description">Serial entrepreneur Jim Marggraf, CEO and Founder, Eyefluence, on problem solving.</p>
      </div>

      <div class="col-12">
        <%= form_for @wad, html: { multipart: true } do |f| %>
          <div class="form-group">
            <%= f.label :category %>
            <%= f.select :category,
                        options_for_select(
                          [
                            "Art",
                            "B2B Saas",
                            "Finance",
                            "Hardware",
                            "Small Business",
                            "Consumer Tech",
                            "Other"
                            ]
                        ),
                        {},
                        class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :problem_state, "What are you trying to solve?" %>
            <%= f.text_field :problem_state, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :short_form, "Describe your solution in about 10 words" %>
            <%= f.text_field :short_form, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :long_form, "Now really think it out (max. 500 words)" %>
            <%= f.text_area :long_form, class: 'form-control' %>
          </div>

          <div class="form-group">
            <%= f.label :image, "Any images?" %>
            <%= f.file_field :image, class: 'form-control-file' %>
          </div>

          <%= f.submit "Submit Wad", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>

</div>
