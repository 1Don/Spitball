<div class="mt-4">
		<div class="">
			<div class="align-items-center mb-4 row-flex col-12">
				<%= render 'category_nav' %>
			</div>
			<% @wads.each do |wad| %>
				<%= render wad %>
			<% end %>
		</div>
</div>
