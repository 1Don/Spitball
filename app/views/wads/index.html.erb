<div class="mt-4">
		<%# The display for all wads%>
		<div class="">
			<div class="align-items-center mb-4 row-flex col-12">
				<%= render 'category_nav' %>
			</div>
			<div class="">
				<% @wads.each do |wad| %>
					<%= render wad %>
				<% end %>
			</div>
		</div>
</div>
