console.log("downvote script running");
if ("<%= @comment.parent_id %>" == ""){
  var comment_like_html = `
    <%= link_to wad_comment_like_path(@comment.wad, @comment), class: "profile-link", method: :put, remote: true do%>
      <div class="row p-2 ">
        <div class="col-2 pl-1 mt-2">
          <%= image_tag('heart-empty.svg') %>
        </div>
        <div class="col-5 small mt-1">
          <div>
            <%= @comment.get_upvotes.size %>
          </div>
        </div>
      </div>
    <% end %>
  `
} else{
  var comment_like_html = `
    <%= link_to wad_comment_like_path(@comment.wad, @comment), class: "profile-link", method: :put, remote: true do%>
      <div class="row p-2 ">
        <div class="col-2 mt-1">
          <%= image_tag('heart-empty.svg') %>
        </div>
        <div class="col-5 small mt-1">
          <div>
            <%= @comment.get_upvotes.size %>
          </div>
        </div>
      </div>
    <% end %>
  `
}

$('#comment-like-<%= @comment.id %>').html(comment_like_html)
